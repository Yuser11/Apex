CREATE DATABASE APEX; USE
    APEX;
CREATE TABLE ENDERECO(
    id INT(11) NOT NULL AUTO_INCREMENT,
    cep CHAR(8) NOT NULL,
    numero CHAR(6) NOT NULL,
    bairro VARCHAR(100) NOT NULL,
    rua VARCHAR(100) NOT NULL,
    estado CHAR(2) NOT NULL,
    cidade VARCHAR(40),
    complemento TEXT,
    PRIMARY KEY(id)
); CREATE TABLE TELEFONE(
    id INT(11) NOT NULL AUTO_INCREMENT,
    movel CHAR(9) NOT NULL,
    fixo CHAR(8) NOT NULL,
    emergencia CHAR(9),
    PRIMARY KEY(id)
); CREATE TABLE EMPRESA(
    id INT(11) NOT NULL AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    cnpj CHAR(14) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    data_criacao DATE NOT NULL,
    observacoes TEXT,
    endereco_id INT(11) NOT NULL,
    telefone_id INT(11) NOT NULL,
    PRIMARY KEY(id),
    CONSTRAINT fk_empresa_endereco FOREIGN KEY(endereco_id) REFERENCES ENDERECO(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_empresa_telefone FOREIGN KEY(telefone_id) REFERENCES TELEFONE(id) ON DELETE RESTRICT ON UPDATE CASCADE
); 
CREATE TABLE PROFISSIONAL(
    id INT(11) NOT NULL AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    data_nascimento DATE NOT NULL,
    cpf CHAR(11) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    senha VARCHAR(150) NOT NULL,
    modalidade VARCHAR(10) NOT NULL,
    data_ingresso DATE NOT NULL,
    data_cadastro DATE NOT NULL DEFAULT(CURRENT_DATE),
    regra VARCHAR(10) NOT NULL,
    endereco_id INT(11) NOT NULL,
    telefone_id INT(11) NOT NULL,
    empresa_id INT(11) NOT NULL,
    PRIMARY KEY(id),
    CONSTRAINT fk_profissional_empresa FOREIGN KEY(empresa_id) REFERENCES EMPRESA(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_profissional_telefone FOREIGN KEY(telefone_id) REFERENCES TELEFONE(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_profissional_endereco FOREIGN KEY(endereco_id) REFERENCES ENDERECO(id) ON DELETE RESTRICT ON UPDATE CASCADE
); CREATE TABLE VEICULO(
    id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    marca VARCHAR(20) NOT NULL,
    ano INT(4) NOT NULL,
    quilometragem INT(6) NOT NULL,
    valor DECIMAL(10, 2) NOT NULL,
    tracao CHAR(3) NOT NULL,
    data_cadastro DATE NOT NULL DEFAULT(CURRENT_DATE),
    descricao TEXT,
    empresa_id INT(11) NOT NULL,
    CONSTRAINT fk_carro_empresa FOREIGN KEY(empresa_id) REFERENCES EMPRESA(id) ON DELETE RESTRICT ON UPDATE CASCADE
); CREATE TABLE CLIENTE(
    id INT(11) NOT NULL AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    cpf CHAR(14) NOT NULL UNIQUE,
    genero CHAR(1) NOT NULL,
    data_nascimento DATE NOT NULL,
    email VARCHAR(100) UNIQUE,
    observacoes TEXT,
    data_cadastro DATETIME DEFAULT(CURRENT_DATE),
    empresa_id INT(11) NOT NULL,
    endereco_id INT(11) NOT NULL,
    telefone_id INT(11) NOT NULL,
    PRIMARY KEY(id),
    CONSTRAINT fk_cliente_empresa FOREIGN KEY(empresa_id) REFERENCES EMPRESA(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_cliente_endereco FOREIGN KEY(endereco_id) REFERENCES ENDERECO(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_cliente_telefone FOREIGN KEY(telefone_id) REFERENCES TELEFONE(id) ON DELETE RESTRICT ON UPDATE CASCADE
); CREATE TABLE COMPRA(
    id INT(11) NOT NULL AUTO_INCREMENT,
    data_compra DATETIME DEFAULT(CURRENT_TIME) NOT NULL,
    cliente_id INT(11) NOT NULL,
    veiculo_id INT(11) NOT NULL,
    empresa_id INT(11) NOT NULL,
    PRIMARY KEY(id),
    CONSTRAINT fk_compra_empresa FOREIGN KEY(empresa_id) REFERENCES EMPRESA(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_compra_cliente FOREIGN KEY(cliente_id) REFERENCES CLIENTE(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_compra_veiculo FOREIGN KEY(veiculo_id) REFERENCES VEICULO(id) ON DELETE RESTRICT ON UPDATE CASCADE
);
CREATE TABLE GALERIA(
    id INT(11) NOT NULL AUTO_INCREMENT,
    imagem VARCHAR(150) NOT NULL,
    cliente_id INT(11),
    empresa_id INT(11),
    profissional_id INT(11),
    veiculo_id INT(11),
    PRIMARY KEY(id),
    CONSTRAINT fk_galeria_empresa FOREIGN KEY(empresa_id) REFERENCES EMPRESA(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_galeria_profissional FOREIGN KEY(profissional_id) REFERENCES PROFISSIONAL(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_galeria_veiculo FOREIGN KEY(veiculo_id) REFERENCES VEICULO(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    CONSTRAINT fk_galeria_cliente FOREIGN KEY(cliente_id) REFERENCES CLIENTE(id) ON DELETE RESTRICT ON UPDATE CASCADE
); 